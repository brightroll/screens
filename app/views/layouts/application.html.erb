<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Airplay Wall</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->
    <%= stylesheet_link_tag :application, :media => "all" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le javascript -->
    <%= javascript_include_tag :application %>

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="assets/favicon.ico">

    <%= csrf_meta_tags %>
  </head>

  <body>

    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <a class="navbar-brand" href="/">Screens</a>
        <ul class="nav navbar-nav">
          <li class="<%= 'active' if params[:controller] == 'devices' %>"><a href="/devices">Devices</a></li>
          <li class="<%= 'active' if params[:controller] == 'slideshows' %>"><a href="/slideshows">Slideshows</a></li>
          <li class="<%= 'active' if params[:controller] == 'slides' %>"><a href="/slides">Slides</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if session[:user] %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= session[:user] %> <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><a href="/auth/logout">Logout</a></li>
            </ul>
          </li>
          <% else %>
          <li><a href="<%= login_path %>">Login</a></li>
          <% end %>
        </ul>
      </div>
    </nav>

    <div class="container">
      <% flash.each do |key, value| -%>
        <div class="alert alert-<%= key %>">
          <a class="close" data-dismiss="alert">&times;</a>
          <%= value %>
        </div>
      <% end -%>

      <%= yield %>

    </div> <!-- /container -->

  </body>
</html>

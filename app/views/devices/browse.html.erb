<%= render :partial => 'breadcrumbs', :object => [
           { :url => '/devices', :name => 'Devices'},
           { :url => '/devices/browse', :name => 'Browse the Network'}
           ] %>

<%= render 'devices' %>

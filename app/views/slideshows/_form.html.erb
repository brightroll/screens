<%= form_for(@slideshow, :html => {:class => 'well form-inline edit_slideshow'}) do |f| %>
  <% if @slideshow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slideshow.errors.count, "error") %> prohibited this slideshow from being saved:</h2>

      <ul>
      <% @slideshow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <legend>Edit Slideshow</legend>

  <%= f.label :name, :class => "control-label" %>
  <%= f.text_field :name, :class => "input-xlarge span4" %>

  <%= f.submit :class => "btn btn-primary" %>

<% end %>
